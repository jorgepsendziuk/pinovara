name: Deploy to Production

# 🚀 PINOVARA Deploy v4.0 - CLEAN & SIMPLE (Versão Corrigida)
# ✅ Lógica de deploy robusta com caminhos corrigidos.
# ✅ Script de execução no servidor mais claro e seguro.

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🚀 Starting Clean Deploy v4.0
      run: |
        echo "🎯 PINOVARA Deploy v4.0 - CLEAN & SIMPLE"
        echo "✅ NO backup system - Direct deployment only"
        echo "✅ NO complexity - Just deploy and go"
        echo "📅 $(date)"

    - name: 🔧 Validate Secrets
      run: |
        if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
          echo "❌ SSH_PRIVATE_KEY secret not configured"
          exit 1
        fi
        if [ -z "${{ secrets.SERVER_HOST }}" ]; then
          echo "❌ SERVER_HOST secret not configured"
          exit 1
        fi
        if [ -z "${{ secrets.SERVER_USER }}" ]; then
          echo "❌ SERVER_USER secret not configured"
          exit 1
        fi
        echo "✅ All secrets configured"

    - name: 📥 Checkout Code
      uses: actions/checkout@v4

    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: 🏗️ Build Backend
      working-directory: ./backend
      run: |
        echo "🔨 Building backend..."
        npm ci
        npm run build
        echo "✅ Backend built successfully"

    - name: 🎨 Build Frontend
      working-directory: ./frontend
      run: |
        echo "🎨 Building frontend..."
        npm ci
        npm run build
        echo "✅ Frontend built successfully"

    - name: 🔐 Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

    - name: 🚀 Deploy to Server
      run: |
        echo "🚀 Starting deployment to ${{ secrets.SERVER_HOST }}..."

        # 1. Criar um diretório temporário no runner do GitHub Actions
        DEPLOY_DIR="/tmp/pinovara-deploy-$(date +%s)"
        mkdir -p $DEPLOY_DIR

        # 2. Copiar todos os artefatos de build para o diretório temporário
        echo "📋 Preparing deployment files..."
        cp -r backend/package*.json backend/dist backend/prisma $DEPLOY_DIR/ 2>/dev/null || true
        mkdir -p $DEPLOY_DIR/frontend-dist
        cp -r frontend/dist/* $DEPLOY_DIR/frontend-dist/ 2>/dev/null || true

        # 3. Criar o arquivo .env no diretório temporário
        if [ -f "backend/config.env" ]; then
          cp backend/config.env $DEPLOY_DIR/.env
        else
          echo 'NODE_ENV=production' > $DEPLOY_DIR/.env
          echo 'PORT=3001' >> $DEPLOY_DIR/.env
          echo 'DATABASE_URL="postgresql://pinovara:pinovara@bd.pinovaraufba.com.br:5432/pinovara?schema=pinovara"' >> $DEPLOY_DIR/.env
          echo 'JWT_SECRET="pinovara-secret-key-change-in-production"' >> $DEPLOY_DIR/.env
        fi

        # 4. Criar a configuração do PM2 no diretório temporário
        cat > $DEPLOY_DIR/ecosystem.config.js << 'EOF'
        module.exports = {
          apps: [{
            name: 'pinovara-backend',
            script: 'dist/server.js',
            cwd: '/var/www/pinovara/backend',
            instances: 1,
            exec_mode: 'fork',
            env_production: {
              NODE_ENV: 'production'
            }
          }]
        };
        EOF

        # 5. Criar o script de deploy (com a lógica corrigida) no diretório temporário
        cat > $DEPLOY_DIR/deploy.sh << 'EOF'
        #!/bin/bash
        set -e

        echo "🚀 PINOVARA v4.0 - CLEAN DEPLOY SCRIPT STARTING ON SERVER..."

        # --- CORREÇÃO PRINCIPAL ---
        # Navega para o diretório onde o script foi salvo no servidor.
        # Isso garante que todos os caminhos relativos (./dist, ./prisma) funcionem.
        cd "$(dirname "$0")"
        echo "📂 Working from temporary directory: $(pwd)"

        # Verifica se o PM2 está instalado
        if ! command -v pm2 &> /dev/null; then
            echo "❌ PM2 not found. Please install it with 'npm install -g pm2'."
            exit 1
        fi
        echo "✅ PM2 is available."

        # --- DEPLOY DO FRONTEND ---
        echo "🎨 Deploying frontend..."
        sudo mkdir -p /var/www/html
        sudo rm -rf /var/www/html/*
        if [ -d "./frontend-dist" ]; then
          sudo cp -r ./frontend-dist/* /var/www/html/
          echo "✅ Frontend deployed successfully."
        else
          echo "⚠️ Frontend build files not found in deployment package."
        fi

        # --- DEPLOY DO BACKEND ---
        echo "🚀 Deploying backend..."
        # Para e deleta o processo antigo para garantir um início limpo
        pm2 stop pinovara-backend 2>/dev/null || true
        pm2 delete pinovara-backend 2>/dev/null || true

        # Prepara o diretório final do backend
        sudo rm -rf /var/www/pinovara/backend
        sudo mkdir -p /var/www/pinovara/backend

        # Copia todos os arquivos do backend do diretório temporário para o destino final
        echo "📋 Copying backend files to /var/www/pinovara/backend..."
        sudo cp -r package*.json dist prisma .env ecosystem.config.js /var/www/pinovara/backend/

        # Navega para o diretório final do backend para executar os próximos comandos
        cd /var/www/pinovara/backend
        echo "📂 Working from final backend directory: $(pwd)"
        
        # Instala dependências de produção
        echo "📦 Installing production dependencies..."
        npm ci --omit=dev

        # Configura o banco de dados com Prisma
        echo "🗄️ Setting up database..."
        npx prisma generate
        npx prisma db push --accept-data-loss

        # Inicia a aplicação com PM2
        echo "🚀 Starting application with PM2..."
        pm2 start ecosystem.config.js --env production
        pm2 save # Salva a configuração para reiniciar junto com o servidor

        echo "🎉 DEPLOYMENT COMPLETED SUCCESSFULLY!"
        EOF

        # 6. Tornar o script executável
        chmod +x $DEPLOY_DIR/deploy.sh

        # 7. Enviar o pacote de deploy para o servidor e executar o script
        scp -r $DEPLOY_DIR ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/tmp/
        ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "bash /tmp/pinovara-deploy-*/deploy.sh"

        # 8. Limpeza dos arquivos temporários no runner e no servidor
        rm -rf $DEPLOY_DIR
        ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'rm -rf /tmp/pinovara-deploy-*'

    - name: ✅ Verify Deployment
      run: |
        echo "🔍 Verifying deployment..."
        sleep 10

        # Verifica o backend
        if ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'curl -f http://localhost:3001/health > /dev/null 2>&1'; then
          echo "✅ Backend is running and healthy"
        else
          echo "❌ Backend not responding on http://localhost:3001/health"
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'pm2 logs pinovara-backend --lines 20'
        fi

        # Verifica o frontend
        if ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'curl -f http://localhost/ > /dev/null 2>&1'; then
          echo "✅ Frontend is serving content on port 80"
        else
          echo "⚠️ Frontend not responding. This might be expected if a web server like Nginx is not configured to serve from /var/www/html."
        fi

        echo "🎉 Deployment verification complete!"